# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

New, empty workspace. No source code, build system, or framework has been set up yet.
**Update this section once a stack is chosen** — replace the placeholders below with the real
architecture and commands.

## Runtime / Tooling

Bun is the intended runtime and package manager for this project (inherited from the parent
workspace conventions).

Common commands — fill in once a `package.json` exists:

- `bun install` — install dependencies
- `bun run <script>` — run a script defined in `package.json`
- `bun test` — run the test suite
- `bun test <file>` — run a single test file

## Relationship to Parent Workspace

`../CLAUDE.md` (`/Users/jiangfeng/VibeCoding/claude/CLAUDE.md`) applies workspace-wide and
defines pre-approved permissions. This file is scoped to `demo1` and should hold details
specific to this project.

## Note for Future Instances

The command placeholders and "Project Status" above are intentional stand-ins for an empty
workspace. As real code is added, replace them with verified commands and a high-level
architecture overview — do not leave fabricated or guessed details.
